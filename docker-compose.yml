services:
  bubo:
    image: bubo
    network_mode: host
    volumes:
      - ./results:/results
      - .:/bubo/input
      - compliance_cache:/bubo/cache
    command: [ "--help" ]

volumes:
  compliance_cache: